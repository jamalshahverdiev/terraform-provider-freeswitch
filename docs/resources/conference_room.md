---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "freeswitch_conference_room Resource - freeswitch"
subcategory: ""
description: |-
  A conference room: the room itself plus the dialplan extension callers dial to enter it (rendered into /xml/dialplan automatically).
---

# freeswitch_conference_room (Resource)

A conference room: the room itself plus the dialplan extension callers dial to enter it (rendered into `/xml/dialplan` automatically).

## Example Usage

```terraform
# A room also materializes the dialplan extension callers dial to enter it.
resource "freeswitch_conference_room" "standup" {
  name    = "standup"
  number  = "3500"
  domain  = "192.168.48.143"
  context = "company"
  profile = freeswitch_conference_profile.video_grid.name
}

# PIN-protected room.
resource "freeswitch_conference_room" "private" {
  name    = "private"
  number  = "3501"
  domain  = "192.168.48.143"
  context = "company"
  profile = freeswitch_conference_profile.video_grid.name
  pin     = "1234"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `context` (String) Dialplan context the entry extension is rendered into.
- `domain` (String)
- `name` (String) Conference name, e.g. `standup`.
- `number` (String) Dialable number, e.g. `3500`.
- `profile` (String) Name of a `freeswitch_conference_profile`.

### Optional

- `enabled` (Boolean)
- `max_members` (Number) 0 = unlimited.
- `pin` (String, Sensitive)
- `priority` (Number) Dialplan ordering — keep lower than broad catch-all extensions.

### Read-Only

- `created_at` (String)
- `id` (String) The ID of this resource.
- `updated_at` (String)

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# freeswitch_conference_room is imported by its name.
terraform import freeswitch_conference_room.standup standup
```
